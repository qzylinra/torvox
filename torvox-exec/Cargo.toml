[package]
name = "torvox-exec"
version = "0.1.0"
edition = "2024"
description = "Multi-call binary for W^X workaround on Android (Termux-proven pattern)"
license = "UNLICENSED"

[[bin]]
name = "torvox-exec"
path = "src/main.rs"

[dependencies]
nix = { workspace = true }
