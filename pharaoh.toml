# Pharaoh configuration — generated by pharaoh-setup algorithm
# Project: torvox
# Mode: reverse-eng (codebase exists, sphinx-needs being created now)

[pharaoh]
strictness = "enforcing"

[pharaoh.id_scheme]
# Inferred from RST tree: REQ_CORE_001, REQ_TERM_001, REQ_REND_001, REQ_ANDR_001
# Observed shape: {TYPE}_{NUMBER}
pattern = "{TYPE}_{NUMBER}"
auto_increment = true

[pharaoh.workflow]
mode = "reverse-eng"
# Gates tuned for reverse-eng — tighten as the catalogue stabilises.
require_change_analysis = false
require_verification = true
require_mece_on_release = false

[pharaoh.traceability]
# Source 3 — refuse to guess (no needs.json built yet).
# Build docs once and re-run pharaoh-setup to infer from needs.json.
required_links = [
  # TODO: build docs to generate needs.json, then re-run pharaoh-setup
  # to infer traceability chains from actual edges.
]

[pharaoh.codelinks]
enabled = true
